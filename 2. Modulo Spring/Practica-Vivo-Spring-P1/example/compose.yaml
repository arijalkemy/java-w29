services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080" # Mapea el puerto del contenedor al host
    environment:
      - SPRING_PROFILES_ACTIVE=default
